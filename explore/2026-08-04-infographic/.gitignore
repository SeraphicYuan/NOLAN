node_modules/
package-lock.json
_dbg.mjs
_probe.mjs
# Render output is reproducible from cases.py; only the two stills the verdict was READ from are
# tracked (see RESULTS.md), because a visual verdict without its evidence is an assertion.
_out/*
!_out/sheet-highlighter.png
!_out/vecfigs/
_out/vecfigs/*
!_out/vecfigs/p13_v0.png
!_out/baseline-final.png
!_out/cutouts.png
_shot.mjs
# Generated bundle: cards are rebuilt by ds_seed.py. The adherence config IS tracked — it is the
# deliverable, and it must be diffable when author.py's rule set changes.
_ds/components/
_ds/readme.md
_b.mjs
_cut_sheet.mjs
# Test D working set: cutouts + VO are regenerable (prep_assets.py, make_vo.py) and heavy.
# narration.json IS tracked — the aligner times are the ground truth every anchor check compares
# against, and a re-synthesis would silently move the bar.
_testD/assets/
_testD/voice/
_testD/*.html
!_testD/narration.json
_p2f/
