# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv/
env/
venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/

# Project specific
*.db

# Scratch/debug directory (temporary outputs, experiments)
.scratch/

# Large project files (videos, audio)
projects/*/output/*.mp4
projects/*/output/*.webm
projects/*/output/*.mov
projects/*/source/*.mp4
projects/*/source/*.webm
projects/*/source/*.mov
projects/*/assets/voiceover/*.mp3
projects/*/assets/voiceover/*.wav

# Vector stores
.template_vectors/
chroma_db/

# Temporary directories
.tmp/

# Node modules (render-service)
render-service/node_modules/
render-service/dist/

# Video analysis projects (local only, large files)
video_analysis/

# ComfyUI sample previews (scratch)
samples/

# Script-style corpora + guides (user data)
script_styles/

# Global generated libraries (picture library, lottie render previews)
/_library/

# Node dependencies (never commit — was causing git add to stall)
node_modules/

# Generated silent audio tracks (render scratch — can exceed GitHub 100MB limit)
silent.wav

# --- Lean repo: generated / regenerable artifacts (2026-06-28) ---
# Test & demo render outputs
test_output/
motion_out/
segment_out/

# Per-project rendered media (keep scene_plan.json / script.md / configs)
projects/**/*.mp4
projects/**/*.webm
projects/**/*.mov
projects/**/*.wav
projects/_clips/
projects/**/.orchestrator/modules/render/

# Beautiful-article build workspaces (regenerable; node_modules already ignored).
# Keeps beautiful-articles/<name>/ source+plan and the human-3.0 test fixture.
beautiful-articles/*-build/
beautiful-articles/_pubtest/

# Render-service scratch
render-service/uploads/
render-service/remotion-lib/output/
render-service/_lab_chapter/output/
render-service/_lab_chapter/public/
render-service/_lab_chapter/jobs/_auto_*.json

# web-video-lab generated artifacts (jobs are regenerable from specs; renders/scratch are large)
web-video-lab/art/*.job.json
web-video-lab/art/_*.png
web-video-lab/flows/_*.png

# project-owned flow build artifact
projects/*/flow.job.json

# project flow build dir (per-beat clips, concat scratch)
projects/*/.flow/

# project render outputs (large)
projects/*/video/
projects/*/scene_plan.json

# fleet runtime state
.nolan/agents/

render-service/remotion-lib/public/
render-service/remotion-lib/jobs/_auto_*.json
.nolan/skills/

# --- untracked-clutter cleanup (2026-07-13): generated/acquired media, never source ---
# Project-library generated / model-sample media (regenerable)
projects/_library/_broll_generated/
projects/_library/_krea2_samples/
projects/_library/_tonal_broll/
# Perceptual / temporal render-gate scratch stills under lab comps
render-service/_lab_hyperframes/videos/*/capture/_perceptual/
render-service/_lab_hyperframes/videos/*/capture/_temporal/
# Stale editor temp files (e.g. *.css.tmp-19692)
*.tmp-*
